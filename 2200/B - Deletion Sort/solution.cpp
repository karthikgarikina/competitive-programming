#include <bits/stdc++.h>using namespace std; #define ll long long int main() {    ios::sync_with_stdio(false);    cin.tie(NULL);    int t;    cin >> t;    while (t--) {        int n;        cin>>n;        int temp,prev=INT_MIN;        bool flag=false;        for(int i=0;i<n;i++){            cin>>temp;            if(temp<prev) flag=true;            prev=temp;        }        if(flag) cout<<"1"<<"\n";        else cout<<n<<"\n";    }     return 0;}