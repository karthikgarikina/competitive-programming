#include <bits/stdc++.h>using namespace std; #define ll long long int main() {    ios::sync_with_stdio(false);    cin.tie(NULL);    int t;    cin >> t;    while (t--) {        int n;        cin>>n;        int temp,maxi=INT_MIN,cnt=0;        for(int i=0;i<n;i++){            cin>>temp;            if(temp>maxi){                maxi=temp;                cnt=1;            }            else if(temp==maxi) cnt++;        }        cout<<cnt<<"\n";    }     return 0;}