#include <bits/stdc++.h>using namespace std; #define ll long long int main() {    ios::sync_with_stdio(false);    cin.tie(NULL);    int t;    cin >> t;    while (t--) {        int n;        cin>>n;        string str;        cin>>str;        stack<char>st;        for(auto ch : str){            if(!st.empty() and st.top()==ch) st.pop();            else st.push(ch);        }        if(st.empty()){            cout<<"YES\n";        }        else cout<<"NO\n";    }     return 0;}